got_eq = gmcp.Char.Vitals.eq
got_bal = gmcp.Char.Vitals.bal

function malfunction()
  if got_eq then
    send("dance")
    return true
  elseif got_bal then
    send("tap")
    return true
  end

  return false
end

svo.addbalanceful("maliki function", malfunction)
